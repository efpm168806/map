<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hideseekapp">

    <!--
     The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
     Google Maps Android API v2, but you must specify either coarse or fine
     location permissions for the 'MyLocation' functionality.
-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".forgetpwd"/>
        <activity android:name="registerpage1"/>
        <activity android:name="registerpage2"/>
        <activity android:name=".agreepage"/>
        <activity android:name=".gamemainpage"/>
        <activity android:name=".personalpage"/>
        <activity android:name=".passwordchange"/>
        <activity android:name=".settingpage"/>
        <activity android:name=".bagpage"/>
        <activity android:name=".shoppage"/>
        <activity android:name=".shoppage2"/>
        <activity android:name=".seekroom"/>
        <activity android:name=".creatroom"/>
        <activity android:name=".inroom"/>
        <activity android:name=".ingame"/>
        <activity android:name=".finishpage"/>
    </application>

</manifest>